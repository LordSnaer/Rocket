<!--
  Route-server-augmented nav2 behavior tree.
  ComputeAndTrackRoute plans a sparse path through the route graph (Jazzy-style
  Route Server backported on Humble), then FollowPath drives the controller.
  No recovery branch: any failure aborts the goal so a stale goal does not get
  retried into the spin/wait/backup loop.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <PipelineSequence name="NavigateWithReplanning">
      <RateController hz="1.0">
        <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
      </RateController>
      <FollowPath path="{path}" controller_id="FollowPath"/>
    </PipelineSequence>
  </BehaviorTree>
</root>
